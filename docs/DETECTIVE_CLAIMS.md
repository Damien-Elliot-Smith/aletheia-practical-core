# Detective Claims Review (Phase 4)

Deterministic, read-only claim review from **case.zip only**.

## Run

```bash
python ag.py detective-claims --case ./case.zip --claim-id c1
python ag.py detective-claims --case ./case.zip --all
```

Outputs a schema-locked logic map for each claim.
