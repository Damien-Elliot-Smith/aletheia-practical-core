# ClaimCheck (Phase 3)

ClaimCheck verifies claims using **case.zip only**.

## Run

```bash
python ag.py claimcheck --case ./case.zip --claim-id c1
python ag.py claimcheck --case ./case.zip --all
```

Verdicts are PASS/FAIL/INCONCLUSIVE.
